export const SM_BREAKPOINT_PX: number = 640;

// Duration in seconds for the visual animation of each part (circle or line) in a MediaObject.
export const MEDIA_OBJECT_ANIMATION_TIME_S: number = 2;

// Delay in seconds between the start of one animation part (circle or line)
// and the start of the next consecutive animation part in the MediaChain.
export const MEDIA_CHAIN_CASCADE_DELAY_S: number = 0.12;
